% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derivatives.R
\name{df_dp}
\alias{df_dp}
\title{Numerical derivative of a matrix-valued function wrt a scalar parameter}
\usage{
df_dp(funct, parm_value, row = NULL, col = NULL, ctx = get_context())
}
\arguments{
\item{funct}{A function \code{f(parm_value, ctx = ctx)} returning a
\code{Matrix}.}

\item{parm_value}{The parameter matrix.}

\item{row, col}{Row and column index of the scalar element to differentiate
with respect to.  May be \code{NULL} for 1-D or scalar parameters.}

\item{ctx}{A \code{DerivativeContext}.  Defaults to
\code{\link{get_context}()}.}
}
\value{
A \code{Matrix} (or numeric) with the same structure as
  \code{funct}'s output, containing \eqn{\partial f / \partial
  \text{parm\_value}_{row,col}}.
}
\description{
Computes (or retrieves from cache) the numerical Jacobian of \code{funct}
with respect to \code{parm_value}, then extracts the column corresponding
to element \code{[row, col]} of \code{parm_value} and formats it to match
the structure of \code{funct}'s output.
}
